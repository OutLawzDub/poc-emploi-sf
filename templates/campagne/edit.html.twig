{% extends 'base.html.twig' %}

{% block title %}Editer une campagne{% endblock %}

{% block body %}
    <br>
    <h1>Editer une campagne</h1>

    {{ include('campagne/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    {{ include('campagne/_delete_form.html.twig') }}

    <br>

    <a class="btn btn-sm btn-info" href="{{ path('campagne_index') }}">
        <i class="fas fa-arrow-left"></i>
        Retour à la liste
    </a>
{% endblock %}
