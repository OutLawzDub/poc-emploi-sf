<form method="post" action="{{ path('horaire_delete', {'id': horaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ horaire.id) }}">
    <br>
    <button class="btn btn-primary btn-danger">Supprimer</button>
</form>
